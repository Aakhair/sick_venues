<div class="my-venues mt-5">
  <div class="container mt-3">
  <div class="d-flex my-5 justify-content-between">
    <h1 class="text-start mb-3 col">My Reservations</h1>
  </div>
    <div class="card-list-horizontal">
      <% @reservations.each do |reservation| %>
        <div class="card justify-content-between rounded-3">
          <div class="row g-2" >
            <div class="col-2 d-flex align-items-center">
              <div class="card-image-container m-auto rounded-4" style="background-image: url('<%= cl_image_path reservation.venue.photo.key %>')">
              </div>
            </div>
            <div class="col-8">
              <div class="card-body">
                <h5 class="card-title"><%= reservation.date %></h5>
                <p class="card-title"><%= reservation.venue.title %></p>
                <p class="card-text"><small class="text-muted"><%= reservation.venue.location %></small></p>
              </div>
            </div>
            <div class="col-2">
              <h3 class="card-price"><%= reservation.venue.price %> €</h3>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>
